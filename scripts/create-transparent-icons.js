import fs from 'fs';
import path from 'path';
import { createCanvas } from 'canvas'; // We will check if canvas or pure SVG/PNG works

// Pure transparent PNG generator using SVG to PNG or Canvas
console.log('Generating transparent PNG icons...');
